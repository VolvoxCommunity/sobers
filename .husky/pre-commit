pnpm lint-staged

# Run tests only when committing to main branch
current_branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$current_branch" = "main" ]; then
  echo "📋 Committing to main - running tests..."
  pnpm test
fi
